"""Core infrastructure: branding, paths, config, database, logging, utils.

This package must stay free of Qt and third-party dependencies so the
whole backend remains importable and testable headlessly.
"""
